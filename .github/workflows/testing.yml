name: 🐶 Dogtor-App
on:
  push:
    branches-ignore:
      - main

jobs:
  testing:
    name: Testing
    runs-on: ubuntu-latest
    steps:
      - name: Install Dependencies
        run: npm i --save-dev
      - name: Testing Code
        run: npm run build
